<% set_meta_tags :description => t('metas.projects.home.description') %>
<% set_meta_tags :keywords => t('metas.projects.home.keywords') %>

<section class="content_column">

  <h1><%= title t('metas.projects.home.all_projects') %> (<%= link_to '+', new_project_path %>)</h1>

  <% @projects.each do |project| %>
    <%= render project %>
  <% end %>

</section>

