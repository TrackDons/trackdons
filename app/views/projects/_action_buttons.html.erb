<%# link_to t('.follow'), track_donation_path, :class => 'button not_active' %>

<% if logged_in? %>
  <% if current_user.following?(@project) %>
    <%# button_to("Un-Follow", project_unfollow_path(@project), :method => :patch, :remote => true, ) %>
    <%= link_to t('.following'), project_unfollow_path(@project),
          :remote => true,
          :method => :patch,
          :class => "button active",
          :id => 'unfollow_button' %>
  <% else %>
    <%# button_to("Follow", project_follow_path(@project), :method => :patch, :remote => true) %>
    <%= link_to t('.follow'), project_follow_path(@project),
          :remote => true,
          :method => :patch,
          :class => "button not_active tipsit",
          :id => 'follow_button',
          title: t('.follow_explanation') %>
  <% end %>
<% else %>
  <%= link_to t('.follow'), login_path,
          :class => "button not_active tipsit",
          :id => 'follow_button',
          title: t('.follow_explanation') %>
<% end %>

<%= link_to t('.trackdon'), '#track_donation', :class => 'button' %>
